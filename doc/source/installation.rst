============
Installation
============

At the command line::

    $ pip install python-sjsclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-sjsclient
    $ pip install python-sjsclient
